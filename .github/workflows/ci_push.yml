# Continuous integration test of any branch.

name: CI
on:
  push:
    branches-ignore:
    - main

jobs:
  # Build Docker image
  build_docker:
    runs-on: ubuntu-latest

    steps:
    - uses: 'actions/checkout@v2'

    - id: 'auth'
      name: 'Authenticate to Google Cloud'
      uses: 'google-github-actions/auth@v0'
      with:
        credentials_json: '${{ secrets.GOOGLE_CREDENTIALS }}'

    - name: Image
      run: |-
<<<<<<< HEAD
        TAG=$(git rev-parse --abbrev-ref HEAD)
        cd docker
        docker build -q -t us.gcr.io/broad-dsp-lrma/lr-longbow:$TAG .
        gcloud auth configure-docker -q
        docker push -q us.gcr.io/broad-dsp-lrma/lr-longbow:$TAG
=======
        export TAG=$(git rev-parse --abbrev-ref HEAD)
        cd docker && docker build -t us.gcr.io/broad-dsp-lrma/lr-longbow:$TAG .

    - run: |
        gcloud auth configure-docker -q

    - name: Build
      run: |-
        docker push us.gcr.io/broad-dsp-lrma/lr-longbow:$TAG
>>>>>>> 53c049c (More fiddling)
